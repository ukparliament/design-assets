<%= content_tag( 'title', sanitize( @page_title || $SITE_TITLE ) ) %>
<%= tag( 'meta', :name => 'description', :content => sanitize( @description ) ) %>

<%# Open Graph -%>
<%= tag( 'meta', :property => 'og:title', :content => sanitize( @page_title || $SITE_TITLE ) ) %>
<%= tag( 'meta', :property => 'og:description', :content => sanitize( @description ) ) %>